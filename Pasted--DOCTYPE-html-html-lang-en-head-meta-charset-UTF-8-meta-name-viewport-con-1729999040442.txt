<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Flipbooks Dashboard</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
    <style>
        .flipbook-card {
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .flipbook-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }
        .stats-card {
            transition: all 0.3s ease;
        }
        .stats-card:hover {
            background-color: #f8fafc;
        }
    </style>
</head>
<body class="bg-gray-50">
    <div class="container mx-auto px-4 py-8">
        <!-- Dashboard Header -->
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-800">My Flipbooks</h1>
                <p class="text-gray-600 mt-2">Manage and view your flipbook collection</p>
            </div>
            <a href="{{ url_for('upload') }}" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg flex items-center">
                <i class="fas fa-plus mr-2"></i> Create New Flipbook
            </a>
        </div>

        <!-- Stats Overview -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="stats-card bg-white p-6 rounded-lg shadow">
                <div class="flex items-center">
                    <div class="p-3 bg-blue-100 rounded-full">
                        <i class="fas fa-book text-blue-600"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-600">Total Flipbooks</p>
                        <h3 class="text-2xl font-bold text-gray-800">{{ flipbooks|length }}</h3>
                    </div>
                </div>
            </div>
            <div class="stats-card bg-white p-6 rounded-lg shadow">
                <div class="flex items-center">
                    <div class="p-3 bg-green-100 rounded-full">
                        <i class="fas fa-clock text-green-600"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-600">Latest Upload</p>
                        <h3 class="text-2xl font-bold text-gray-800">
                            {% if flipbooks %}
                                {{ flipbooks[0].created_at.strftime('%b %d') }}
                            {% else %}
                                -
                            {% endif %}
                        </h3>
                    </div>
                </div>
            </div>
            <div class="stats-card bg-white p-6 rounded-lg shadow">
                <div class="flex items-center">
                    <div class="p-3 bg-purple-100 rounded-full">
                        <i class="fas fa-eye text-purple-600"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-600">Total Views</p>
                        <h3 class="text-2xl font-bold text-gray-800">0</h3>
                    </div>
                </div>
            </div>
        </div>

        <!-- Flipbooks Grid -->
        {% if flipbooks %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for flipbook in flipbooks %}
            <div class="flipbook-card bg-white rounded-lg shadow overflow-hidden">
                <div class="p-6">
                    <div class="flex justify-between items-start mb-4">
                        <h3 class="text-xl font-semibold text-gray-800">{{ flipbook.title }}</h3>
                        <div class="dropdown">
                            <button class="text-gray-400 hover:text-gray-600">
                                <i class="fas fa-ellipsis-v"></i>
                            </button>
                        </div>
                    </div>
                    <div class="flex items-center text-gray-600 text-sm mb-4">
                        <i class="far fa-calendar-alt mr-2"></i>
                        <span>Created {{ flipbook.created_at.strftime('%B %d, %Y') }}</span>
                    </div>
                    <div class="flex items-center justify-between mt-4">
                        <a href="{{ url_for('viewer', unique_id=flipbook.unique_id) }}" 
                           class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg flex items-center">
                            <i class="fas fa-play mr-2"></i> View Flipbook
                        </a>
                        <div class="flex items-center text-gray-600">
                            <i class="far fa-eye mr-1"></i>
                            <span>0 views</span>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        {% else %}
        <!-- Empty State -->
        <div class="text-center py-12">
            <div class="bg-gray-100 inline-block p-4 rounded-full mb-4">
                <i class="fas fa-book text-4xl text-gray-400"></i>
            </div>
            <h3 class="text-xl font-semibold text-gray-800 mb-2">No flipbooks yet</h3>
            <p class="text-gray-600 mb-6">Create your first flipbook to get started</p>
            <a href="{{ url_for('upload') }}" 
               class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg inline-flex items-center">
                <i class="fas fa-plus mr-2"></i> Create New Flipbook
            </a>
        </div>
        {% endif %}
    </div>
</body>
</html>